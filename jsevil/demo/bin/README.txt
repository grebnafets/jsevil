main is self contained and contains all of the dependencies it needs.
lib is not self contained and does not contain any of the dependencies it needs.
unit is self contained and contains all unit tests and runs them.
